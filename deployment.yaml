apiVersion: v1
kind: Pod
metadata:
  name: simple-api
  labels:
    name: simple-api
spec:
  containers:
  - name: simple-api
    image: serverlab/simple-api:$VERSION
    ports:
    - containerPort: 3000
